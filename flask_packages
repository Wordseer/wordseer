# This file lists all the packages used in the application.
# There is probably a better way to do this, but I don't know of one at the
# moment.
#
# Use bash flask_packages to install everything (make sure your venv is activated)

pip install flask flask-login flask-openid sqlalchemy sqlalchemy-migrate flask-sqlalchemy flask_whooshalchemy==0.54a flask-wtf pytz flask-babel flup inflect mock lxml unidecode pexpect
